# Barebones CLJS

Small ClojureScript starter using Shadow CLJS and an nREPL alias generated by Neil.

## Development

Start the browser build, dev server, and Shadow nREPL:

```sh
npm run dev
```

Open <http://localhost:8000/index.html>. Shadow CLJS starts its nREPL on port `7888`.

Run the Neil-generated JVM nREPL alias directly:

```sh
npm run nrepl
```

Compile once:

```sh
npm run compile
```
